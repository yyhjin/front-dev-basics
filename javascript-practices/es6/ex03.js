/**
 * template literal
 */

const no = 10;
const name = "둘리";
const email = "dooly@gmail.com";

// ex01
console.log("no: " + no + ", name: " + name + ", email: " + email);
console.log(`no: ${no}, name: ${name}, email: ${email}`);

// ex02: 다중행
console.log(`
no: ${no}
name: ${name}
email: ${email}
`);
